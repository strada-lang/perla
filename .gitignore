# Build configuration (generated by ./configure)
/config.mk
/perla.conf
CLAUDE.md
# Compiler + tool binaries
/perla
/perla-cpan
/perla-xs
/perla-build
/test_stash

# Combined single-file build artifact
/lib/Perla/Combined.strada

# Object files, archives, shared libs
*.o
*.a
*.so

# Strada export-metadata sidecars (no longer emitted by current strada -M,
# which embeds the metadata in .rodata; keep stale ones out of the repo)
*.smeta

# Generated C / XS intermediates
*.c.xs.c
*.xs.c

# Test scratch
/t/*.c.xs.c
/scratch/

# Editor / OS cruft
*~
*.swp
.DS_Store

# REPL-JIT Phase 0 harness binary
/experiments/repl_jit/host
