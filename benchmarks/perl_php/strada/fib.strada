package main;
func fib(int $n) int {
    if ($n <= 1) { return $n; }
    return fib($n - 1) + fib($n - 2);
}
func main() int {
    my int $n = 35;
    print("fib(" . $n . ") = " . fib($n) . "\n");
    return 0;
}
